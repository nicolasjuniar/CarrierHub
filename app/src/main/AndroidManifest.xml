<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juniar.carrierhub">

    <application
        android:name=".MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity"/>
        <activity
            android:name=".home.HomeActivity"
            android:label="@string/login_text"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".splashscreen.SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.karyawan.EditKaryawanActivity"
            android:label="@string/edit_karyawan"/>
        <activity android:name=".home.buku.PengelolaanBarangActivity"
            android:label="@string/edit_barang"/>
    </application>

</manifest>